# 💻 КОМАНДИ ДЛЯ РОБОТИ З ПРОЄКТОМ

## 📦 Встановлення

### Перший запуск:
```bash
# Перейдіть в папку проєкту
cd hotel-website

# Встановіть залежності
npm install
```

## 🚀 Запуск

### Режим розробки (development):
```bash
npm run dev
```
- Запускає локальний сервер
- Автоматично перезавантажується при змінах
- Доступний за адресою: http://localhost:5173

### Збірка для production:
```bash
npm run build
```
- Створює оптимізовану збірку в папці `dist/`
- Готова для завантаження на хостинг

### Перегляд production збірки:
```bash
npm run preview
```
- Показує як буде виглядати сайт після збірки
- Доступний за адресою: http://localhost:4173

## 🔧 Додаткові команди

### Очищення кешу та перевстановлення:
```bash
rm -rf node_modules package-lock.json
npm install
```

### Перевірка версії Node.js:
```bash
node --version
```
Має бути >= 16.0.0

### Перевірка версії npm:
```bash
npm --version
```

## 📂 Структура після встановлення:

```
hotel-website/
├── node_modules/      # Встановлені залежності (створюється автоматично)
├── dist/              # Production збірка (створюється після npm run build)
├── src/               # Вихідні файли
├── index.html
├── package.json
└── vite.config.js
```

## ⚠️ Можливі помилки та рішення:

### Помилка: "command not found: npm"
**Рішення:** Встановіть Node.js з https://nodejs.org

### Помилка: "Cannot find module"
**Рішення:** 
```bash
rm -rf node_modules
npm install
```

### Помилка: "Port 5173 is already in use"
**Рішення:** 
- Закрийте інші проєкти на Vite
- Або вкажіть інший порт в `vite.config.js`

### Зміни не відображаються:
**Рішення:**
- Ctrl+C (зупинити сервер)
- npm run dev (запустити знову)
- Оновіть сторінку в браузері (Ctrl+F5)

## 🌐 Деплой на хостинг:

### Netlify:
```bash
npm run build
# Завантажте папку dist/ на Netlify
```

### Vercel:
```bash
npm run build
# Завантажте папку dist/ на Vercel
```

### GitHub Pages:
```bash
npm run build
# Завантажте вміст dist/ в гілку gh-pages
```

## 📝 Порядок роботи:

1. **Розробка:**
   ```bash
   npm run dev
   ```

2. **Редагування:**
   - Змінюйте файли в `src/`
   - Зміни відображаються автоматично

3. **Тестування:**
   - Перевірте на різних розмірах екрану
   - Перевірте всі секції

4. **Збірка:**
   ```bash
   npm run build
   ```

5. **Перегляд збірки:**
   ```bash
   npm run preview
   ```

6. **Деплой:**
   - Завантажте папку `dist/` на хостинг

## 🎯 Швидкий чеклист:

- [ ] Node.js встановлено
- [ ] `npm install` виконано успішно
- [ ] `npm run dev` працює
- [ ] Сайт відкривається в браузері
- [ ] Змінено дані в `config.js`
- [ ] Замінено фото
- [ ] Перевірено на мобільному
- [ ] `npm run build` створив збірку
- [ ] Готово до деплою!

---

**Успіхів! 🚀**
